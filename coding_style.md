```cpp
// cpp file header

// nxi headers
// nxw headers
// ui headers
// STL headers
// Qt headers

// nxi forward declarations
// ui forward declarations
// Qt forward declarations

namespace nxi {
    class zeta : public parent
    {
        // private type aliases
        using type = std::string;
        
    public:
        // public type aliases
        using type = std::string;
    
        zeta();
        ~zeta() = qualifier;
        zeta(const zeta&) = qualifier;
        zeta(zeta&&) = qualifier;
        
        // member functions, grouped by logic
        // virtual write member functions
        void write() override;
        
        // virtual read member functions
        void read() const override;
        
        // nxi write member functions
        
        // nxi read member functions
        
    signals:
        // qt signals
        void event_signal1();
        void event_signal2();
        
    protected:
        
    private:
        // private member variables
    
        // member variables, grouped by logic
    };
} // nxi
```